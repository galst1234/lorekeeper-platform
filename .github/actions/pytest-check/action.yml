name: Pytest
description: Run tests with pytest

inputs:
  project-dir:
    description: Service directory
    required: true

runs:
  using: composite
  steps:
    - run: uv run pytest
      shell: bash
      working-directory: ${{ inputs.project-dir }}
